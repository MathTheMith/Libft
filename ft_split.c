#include "unistd.h"
#include <stdlib.h>
#include <stdio.h>

char **ft_split(char const *s, char c)
{
	size_t i;
	char *ptr;
	const char *str;

	str = s;
        i = 0;
	while (s[i])
	{
		if
	}
        while (s[i])
	{
		if (s[0] == c)
			i--;			
		i++;
	}
	if (s[i - 1] == c)
	       i--;
	ptr = malloc(i);
	i = 0;
	while s
        return(i);
}
